# Insert-path audit — 2026-06-06

Generated by `scripts/audit_insert_paths.py` as a Phase 1.5
deliverable. Every row below is an `INSERT INTO` in `app/tasks/*`.
Strategy column tells you what would happen if a duplicate landed:
* `DO NOTHING` — duplicate silently skipped (idempotent).
* `UPSERT (DO UPDATE)` — duplicate updates the existing row.
* `NONE` — caller must guard; uniqueness constraint will throw.

Before adding a new UNIQUE index in Phase 7 or 9b, cross-check
this list — a UNIQUE that the existing INSERT can't ON CONFLICT
against is what broke migration 022 in 2026.

## app/tasks/ai_process.py

| Line | Table | Strategy |
|---|---|---|
| 402 | `speakers` | NONE — caller must guard against duplicates |
| 430 | `slides` | UPSERT (DO UPDATE) |
| 458 | `segments` | NONE — caller must guard against duplicates |
| 802 | `words` | NONE — caller must guard against duplicates |

## app/tasks/align.py

| Line | Table | Strategy |
|---|---|---|
| 150 | `speakers` | NONE — caller must guard against duplicates |
| 250 | `alignments` | UPSERT (DO UPDATE) |
| 315 | `validation_results` | NONE — caller must guard against duplicates |
| 366 | `audit_events` | NONE — caller must guard against duplicates |

## app/tasks/burn_captions.py

| Line | Table | Strategy |
|---|---|---|
| 340 | `artifacts` | NONE — caller must guard against duplicates |

## app/tasks/fusion.py

| Line | Table | Strategy |
|---|---|---|
| 75 | `slide_time_ranges` | NONE — caller must guard against duplicates |
| 165 | `slide_time_ranges` | NONE — caller must guard against duplicates |
| 202 | `replay_log` | NONE — caller must guard against duplicates |

## app/tasks/help_tasks.py

| Line | Table | Strategy |
|---|---|---|
| 122 | `help_article_versions` | NONE — caller must guard against duplicates |
| 136 | `audit_events` | NONE — caller must guard against duplicates |
| 712 | `help_articles` | NONE — caller must guard against duplicates |

## app/tasks/kp_task.py

| Line | Table | Strategy |
|---|---|---|
| 123 | `key_points_annotations` | UPSERT (DO UPDATE) |
| 269 | `instructor_profiles` | UPSERT (DO UPDATE) |
| 326 | `session_instructor_map` | UPSERT (DO UPDATE) |
| 362 | `session_patterns` | UPSERT (DO UPDATE) |

## app/tasks/lcs_discrepancies.py

| Line | Table | Strategy |
|---|---|---|
| 124 | `transcription_discrepancies` | NONE — caller must guard against duplicates |
| 152 | `word_alignment` | UPSERT (DO UPDATE) |

## app/tasks/normalize.py

| Line | Table | Strategy |
|---|---|---|
| 186 | `normalization_results` | UPSERT (DO UPDATE) |

## app/tasks/slide_extract.py

| Line | Table | Strategy |
|---|---|---|
| 98 | `slides` | UPSERT (DO UPDATE) |
| 122 | `bullets` | NONE — caller must guard against duplicates |
| 285 | `slides` | UPSERT (DO UPDATE) |
| 314 | `bullets` | UPSERT (DO UPDATE) |
| 396 | `slides` | UPSERT (DO UPDATE) |
| 420 | `bullets` | UPSERT (DO UPDATE) |

## app/tasks/sop_tasks.py

| Line | Table | Strategy |
|---|---|---|
| 81 | `sop_state` | NONE — caller must guard against duplicates |
| 93 | `sop_transitions` | NONE — caller must guard against duplicates |
| 118 | `audit_events` | NONE — caller must guard against duplicates |
| 314 | `audit_events` | NONE — caller must guard against duplicates |
| 519 | `audit_events` | NONE — caller must guard against duplicates |

## app/tasks/transcribe.py

| Line | Table | Strategy |
|---|---|---|
| 119 | `segments` | NONE — caller must guard against duplicates |
| 155 | `words` | UPSERT (DO UPDATE) |

## app/tasks/upload_watchdog.py

| Line | Table | Strategy |
|---|---|---|
| 151 | `session_audit` | UPSERT (DO UPDATE) |

